<div class="card-item col">
  <a href="{% if recipe.redirect %}{{ recipe.redirect }}{% else %}{{ recipe.url | relative_url }}{% endif %}">
    <div class="card hoverable">
      <div class="row g-0">
        {% if recipe.img %}
          <div class="card-img col-md-6">
            {% include figure.liquid loading="eager" path=recipe.img sizes="(min-width: 768px) 156px, 50vw" alt="recipe thumbnail" %}
          </div>
        {% endif %}
        <div class="{% if recipe.img %}col-md-6{% else %}col-md-12{% endif %}">
          <div class="card-body">
            <h3 class="card-title text-lowercase">{{ recipe.title }}</h3>
            <p class="card-text">{{ recipe.description }}</p>
            <div class="row ml-1 mr-1 p-0">
              {% if recipe.github %}
                <div class="github-icon">
                  <div class="icon" data-toggle="tooltip" title="Code Repository">
                    <a href="{{ recipe.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
                  </div>
                  {% if recipe.github_stars %}
                    <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                      <i class="fa-solid fa-star"></i>
                      <span id="{{ recipe.github_stars }}-stars"></span>
                    </span>
                  {% endif %}
                </div>
              {% endif %}
            </div>
          </div>
        </div>
      </div>
    </div>
  </a>
</div>
